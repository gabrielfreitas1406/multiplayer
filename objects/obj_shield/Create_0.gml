//Cria o escudo

shield_time = 300;  // 300 frames (5 segundos), você pode ajustar conforme necessário

// Posiciona o escudo em um local aleatório na sala
//x = irandom_range(40, room_width - 40);
//y = irandom_range(40, room_height - 40);

image_speed = 0.2; 