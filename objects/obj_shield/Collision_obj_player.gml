// Se o jogador (obj_player) colidir com o escudo

/*if (place_meeting(x, y, obj_player))
{
    // Ativa o escudo no jogador
    obj_player.shield_active = true;

    // Define o tempo para desativar o escudo no jogador
    obj_player.shield_time = 300;  // 5 segundos (300 frames)
	

    // Destrói o escudo após o jogador pegar
    instance_destroy();
}*/
